#!/bin/bash
#
# Script de instalación y configuración de entorno de trabajo.
# Basado en Debian/Ubuntu. Adhiere a Google Shell Style Guide y Principios SRE.
# --- CONFIGURACIÓN DE SEGURIDAD ---
set -uo pipefail

# --- CONSTANTES Y VARIABLES ---
readonly RED='\033[0;31m'
readonly GREEN='\033[0;32m'
readonly YELLOW='\033[1;33m'
readonly BLUE='\033[0;34m'
readonly NC='\033[0m'
readonly LOG_FILE="$HOME/install_log.txt"
readonly DOTFILES_DIR="./dotfiles"

# Arrays globales para el reporte final
declare -a INSTALLED_PACKAGES=()
declare -a FAILED_PACKAGES=()

# --- FUNCIONES DE UTILIDAD ---

function log() {
  local timestamp
  local message="$1"
  timestamp=$(date +'%Y-%m-%d %H:%M:%S')
  echo "[$timestamp] INFO: $message" >>"$LOG_FILE"
  echo -e "${BLUE}[INFO]${NC} $message"
}

function error() {
  local timestamp
  local message="$1"
  timestamp=$(date +'%Y-%m-%d %H:%M:%S')
  echo "[$timestamp] ERROR: $message" >>"$LOG_FILE"
  echo -e "${RED}[ERROR]${NC} $message" >&2
}

function success() {
  local message="$1"
  echo -e "${GREEN}[OK]${NC} $message"
}

function check_root() {
  if ! sudo -v; then
    error "Este script requiere privilegios sudo. Verifica tus credenciales."
    exit 1
  fi
  (while true; do
    sudo -n true
    sleep 60
    kill -0 "$$" || exit
  done 2>/dev/null) &
}

function run_install() {
  local pkg_name="$1"
  local cmd="$2"

  log "Instalando: $pkg_name..."

  if eval "$cmd" >>"$LOG_FILE" 2>&1; then
    success "$pkg_name instalado correctamente."
    INSTALLED_PACKAGES+=("$pkg_name")
    return 0
  else
    error "Fallo al instalar $pkg_name. Revisa $LOG_FILE para detalles."
    FAILED_PACKAGES+=("$pkg_name")
    return 1
  fi
}

function ask_user() {
  local prompt="$1"
  local response
  read -p "$(echo -e "${YELLOW}$prompt [s/N]: ${NC}")" response
  if [[ "$response" =~ ^[sS]$ ]]; then
    return 0
  else
    return 1
  fi
}

# --- GRUPOS DE INSTALACIÓN ---

function group_core_system() {
  log "=== Grupo: Sistema Base y Dependencias ==="
  run_install "System Update" "sudo apt update && sudo apt upgrade -y"

  local core_pkgs="build-essential libssl-dev zlib1g-dev libbz2-dev \
    libreadline-dev libsqlite3-dev curl git libncursesw5-dev \
    xz-utils tk-dev libxml2-dev libxmlsec1-dev libffi-dev \
    liblzma-dev make gcc ripgrep fd-find unzip xclip \
    python3-venv python3-pip npm pipx wget ncdu \
    libimage-exiftool-perl imagemagick rsyslog rsync zsh zsh-syntax-highlighting zsh-autosuggestions"

  run_install "Dependencias Core" "sudo apt install -y $core_pkgs"

  if [ "$SHELL" != "/usr/bin/zsh" ]; then
    run_install "Cambio a ZSH" "chsh -s $(which zsh)"
  fi
}

function group_i3_desktop() {
  log "=== Grupo: Entorno Gráfico (i3wm & LightDM) ==="

  if ask_user "¿Instalar entorno gráfico (Xorg, i3wm, LightDM y utilidades)?"; then
    local cmd_base="sudo DEBIAN_FRONTEND=noninteractive apt install -y xorg xbacklight xbindkeys xinput i3 i3blocks i3lock lightdm"
    run_install "Xorg, i3wm y LightDM" "$cmd_base"
    run_install "Servicio LightDM" "sudo systemctl enable lightdm"

    local cmd_utils="sudo apt install -y polybar sxhkd picom feh dunst lxpolkit rofi flameshot libnotify-bin fonts-font-awesome fonts-recommended"
    run_install "Utilidades i3 y Fuentes" "$cmd_utils"

    local cmd_apps="sudo apt install -y ranger qimgv pulsemixer pamixer gsimplecal pavucontrol mpv lxappearance galculator pipewire pipewire-audio pipewire-pulse wireplumber"
    run_install "Apps GUI/CLI y Servidor de Audio" "$cmd_apps"

    local cmd_dirs="mkdir -p \"$HOME/.config/i3/scripts\" \
                             \"$HOME/.config/i3/polybar\" \
                             \"$HOME/.config/i3/sxhkd\" \
                             \"$HOME/.config/i3/picom\" \
                             \"$HOME/.config/i3/dunst\" \
                             \"$HOME/.config/i3/rofi\" \
                             \"$HOME/.config/i3/wallpaper\" \
                             \"$HOME/.config/qimgv\" \
                             \"$HOME/Screenshots\""
    run_install "Estructura de directorios" "$cmd_dirs"
  fi
}

function group_appearance() {
  log "=== Grupo: Apariencia (Nerd Fonts y Temas GTK) ==="

  if ask_user "¿Instalar fuentes (Nerd Fonts) y temas (Orchis/Colloid)?"; then
    # 1. Instalar Nerd Fonts (Sólo las requeridas por tu config de Polybar/Terminal)
    local font_dir="$HOME/.local/share/fonts"
    mkdir -p "$font_dir"
    local fonts=("JetBrainsMono" "RobotoMono" "Hack")

    for font in "${fonts[@]}"; do
      if [ ! -d "$font_dir/$font" ]; then
        local cmd_font="wget -qO /tmp/${font}.zip https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/${font}.zip && \
                        unzip -qo /tmp/${font}.zip -d \"$font_dir/$font/\" && \
                        rm /tmp/${font}.zip"
        run_install "Nerd Font: $font" "$cmd_font"
      fi
    done
    run_install "Actualizar caché de fuentes" "fc-cache -f"

    # 2. Instalar Temas GTK e Iconos
    local theme_cmd="mkdir -p /tmp/themes_install && cd /tmp/themes_install && \
      git clone -q --depth 1 https://github.com/vinceliuice/Orchis-theme && \
      cd Orchis-theme && yes | ./install.sh -l -c dark -t grey --tweaks black && \
      cd /tmp/themes_install && git clone -q --depth 1 https://github.com/vinceliuice/Colloid-icon-theme && \
      cd Colloid-icon-theme && ./install.sh -t grey -s dracula && \
      rm -rf /tmp/themes_install"

    run_install "Temas Orchis & Colloid" "$theme_cmd"

    # 3. Aplicar temas automáticamente
    local apply_cmd="mkdir -p ~/.config/gtk-3.0 && \
      echo -e '[Settings]\ngtk-theme-name=Orchis-Grey-Dark\ngtk-icon-theme-name=Colloid-Grey-Dracula-Dark\ngtk-font-name=Sans 10\ngtk-cursor-theme-name=Adwaita\ngtk-xft-antialias=1\ngtk-xft-hinting=1\ngtk-xft-hintstyle=hintfull' > ~/.config/gtk-3.0/settings.ini && \
      echo -e 'gtk-theme-name=\"Orchis-Grey-Dark\"\ngtk-icon-theme-name=\"Colloid-Grey-Dracula-Dark\"\ngtk-font-name=\"Sans 10\"\ngtk-cursor-theme-name=\"Adwaita\"\ngtk-xft-antialias=1\ngtk-xft-hinting=1\ngtk-xft-hintstyle=\"hintfull\"' > ~/.gtkrc-2.0 && \
      gsettings set org.gnome.desktop.interface gtk-theme 'Orchis-Grey-Dark' && \
      gsettings set org.gnome.desktop.interface icon-theme 'Colloid-Grey-Dracula-Dark'"

    run_install "Aplicar Configuración GTK" "$apply_cmd"
  fi
}

function group_wezterm() {
  log "=== Grupo: Terminal (WezTerm) ==="
  if ask_user "¿Instalar WezTerm?"; then
    if ! command -v wezterm &>/dev/null; then
      local cmd_wez="curl -fsSL https://apt.fury.io/wez/gpg.key | sudo gpg --yes --dearmor -o /usr/share/keyrings/wezterm-fury.gpg && \
        echo 'deb [signed-by=/usr/share/keyrings/wezterm-fury.gpg] https://apt.fury.io/wez/ * *' | sudo tee /etc/apt/sources.list.d/wezterm.list > /dev/null && \
        sudo apt update && sudo apt install -y wezterm && \
        sudo update-alternatives --install /usr/bin/x-terminal-emulator x-terminal-emulator /usr/bin/wezterm 50 && \
        sudo update-alternatives --set x-terminal-emulator /usr/bin/wezterm"

      run_install "WezTerm" "$cmd_wez"
    else
      log "WezTerm ya está instalado."
    fi
  fi
}

function group_virtualization() {
  log "=== Grupo: Virtualización (KVM/QEMU) ==="
  if dpkg -l | grep -q qemu-system; then
    log "KVM ya parece estar instalado."
    INSTALLED_PACKAGES+=("KVM (Preexistente)")
    return
  fi

  local cmd="sudo apt install -y qemu-system libvirt-clients libvirt-daemon-system virt-manager && \
             sudo systemctl enable --now libvirtd && \
             sudo usermod -aG libvirt \"$(whoami)\" && \
             sudo usermod -aG kvm \"$(whoami)\""
  run_install "KVM/QEMU" "$cmd"
}

function group_flatpak_notes() {
  log "=== Grupo: Flatpaks (Obsidian, Dropbox, Discord) ==="
  if ask_user "¿Instalar Aplicaciones Flatpak (Obsidian, Dropbox, Discord)?"; then
    if ! command -v flatpak &>/dev/null; then
      run_install "Gestor Flatpak" "sudo apt install -y flatpak"
    fi
    local cmd_flathub="sudo flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo"
    run_install "Repositorio Flathub" "$cmd_flathub"

    if ! flatpak list | grep -q md.obsidian.Obsidian; then
      run_install "Obsidian (Flatpak)" "sudo flatpak install -y flathub md.obsidian.Obsidian"
    fi

    if ! flatpak list | grep -q com.dropbox.Client; then
      run_install "Dropbox (Flatpak)" "sudo flatpak install -y flathub com.dropbox.Client"
    fi

    if ! flatpak list | grep -q com.discordapp.Discord; then
      run_install "Discord (Flatpak)" "sudo flatpak install -y flathub com.discordapp.Discord"
    fi
  fi
}

function group_network_security() {
  log "=== Grupo: Red, VPN y Seguridad ==="
  if ask_user "¿Instalar y configurar SSH, Fail2ban, UFW y sshpass?"; then
    run_install "Paquetes de Seguridad" "sudo apt install -y openssh-server fail2ban ufw sshpass"

    local cmd_ufw="sudo ufw default deny incoming && \
                   sudo ufw default allow outgoing && \
                   sudo ufw allow ssh/tcp && \
                   sudo ufw --force enable"
    run_install "Configuración UFW" "$cmd_ufw"
    run_install "Servicios SSH/Fail2ban" "sudo systemctl enable --now ssh fail2ban"
  fi

  if ask_user "¿Instalar Tailscale (Mesh VPN)?"; then
    if ! command -v tailscale &>/dev/null; then
      run_install "Tailscale" "curl -fsSL https://tailscale.com/install.sh | sh"
    else
      log "Tailscale ya está instalado."
    fi
  fi

  if ask_user "¿Instalar soporte nativo para OpenVPN/WireGuard?"; then
    run_install "OpenVPN & WireGuard" "sudo apt install -y openvpn wireguard network-manager-openvpn"
  fi
}

function group_docker() {
  log "=== Grupo: Contenedores (Docker) ==="
  if ask_user "¿Instalar y configurar Docker Engine?"; then
    if ! command -v docker &>/dev/null; then
      local cmd_docker="sudo install -m 0755 -d /etc/apt/keyrings && \
        sudo curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc && \
        sudo chmod a+r /etc/apt/keyrings/docker.asc && \
        echo \"deb [arch=\$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian \$(. /etc/os-release && echo \"\$VERSION_CODENAME\") stable\" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null && \
        sudo apt update && \
        sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin && \
        sudo usermod -aG docker \"\$(whoami)\""

      if run_install "Docker Engine" "$cmd_docker"; then
        log "Usuario añadido al grupo 'docker'. (Requerirá reiniciar sesión para surtir efecto)."
      fi
    else
      log "Docker ya está instalado."
    fi
  fi
}

function group_dev_tools() {
  log "=== Grupo: Herramientas de Desarrollo (Shell/Nvim) ==="
  if ask_user "¿Configurar entorno Shell (ZSH/Starship/Pyenv)?"; then
    run_install "Direnv" "sudo apt install -y direnv"
    if [[ ! -d "$HOME/.pyenv" ]]; then
      run_install "Pyenv" "curl https://pyenv.run | bash"
    fi
    if ! command -v starship &>/dev/null; then
      run_install "Starship" "curl -sS https://starship.rs/install.sh | sh -s -- -y"
    fi
  fi

  if ask_user "¿Instalar Neovim + LazyVim?"; then
    local cmd_nvim="curl -LO https://github.com/neovim/neovim/releases/latest/download/nvim-linux-x86_64.tar.gz && \
      sudo rm -rf /opt/nvim* && \
      sudo tar -C /opt -xzf nvim-linux-x86_64.tar.gz && \
      sudo ln -sf /opt/nvim-linux-x86_64/bin/nvim /usr/local/bin/nvim && \
      rm nvim-linux-x86_64.tar.gz"

    if run_install "Neovim Binary" "$cmd_nvim"; then
      if [[ -d "$HOME/.config/nvim" ]]; then
        mv "$HOME/.config/nvim" "$HOME/.config/nvim.bak.$(date +%s)"
      fi
      run_install "LazyVim Clone" "git clone https://github.com/LazyVim/starter \"$HOME/.config/nvim\" && rm -rf \"$HOME/.config/nvim/.git\""
    fi
  fi

  if ask_user "¿Instalar Extras (Lazygit, Visidata, Zathura)?"; then
    run_install "Lazygit" "sudo apt install -y lazygit"
    run_install "Visidata" "pipx ensurepath && pipx install visidata --include-deps"
    run_install "Zathura + Poppler" "sudo apt install zathura zathura-pdf-poppler"
  fi
}

function group_dotfiles() {
  log "=== Grupo: Dotfiles ==="
  if ask_user "¿Aplicar Dotfiles desde $DOTFILES_DIR?"; then
    if [[ -d "$DOTFILES_DIR" ]]; then
      sudo apt install -y rsync &>/dev/null
      local target_wallpaper_dir="$HOME/.config/i3/wallpaper"
      local cmd_dot="rm -rf \"$target_wallpaper_dir\" && "
      cmd_dot="${cmd_dot}rsync -a \"$DOTFILES_DIR/\" \"$HOME/\""
      run_install "Dotfiles (Sincronización)" "$cmd_dot"
    else
      error "Directorio dotfiles no encontrado. Saltando."
      FAILED_PACKAGES+=("Dotfiles (Dir not found)")
    fi
  fi
}

function group_cleanup() {
  log "=== Grupo: Limpieza del Sistema ==="
  if ask_user "¿Ejecutar limpieza final (remover dependencias huérfanas)?"; then
    local cmd_clean="sudo apt autoremove -y && sudo apt clean"
    run_install "Limpieza de paquetes" "$cmd_clean"
  fi
}

# --- REPORTE Y FINALIZACIÓN ---

function print_summary() {
  echo ""
  echo -e "${BLUE}========================================${NC}"
  echo -e "${BLUE}       REPORTE FINAL DE INSTALACIÓN     ${NC}"
  echo -e "${BLUE}========================================${NC}"

  echo -e "\n${GREEN}--- Instalado con éxito ---${NC}"
  if [[ ${#INSTALLED_PACKAGES[@]} -eq 0 ]]; then
    echo "  (Ninguno)"
  else
    for pkg in "${INSTALLED_PACKAGES[@]}"; do
      echo -e "  [+] $pkg"
    done
  fi

  echo -e "\n${RED}--- Fallos de instalación ---${NC}"
  if [[ ${#FAILED_PACKAGES[@]} -eq 0 ]]; then
    echo "  (Ninguno)"
  else
    for pkg in "${FAILED_PACKAGES[@]}"; do
      echo -e "  [!] $pkg"
    done
    echo -e "\n${YELLOW}Revisa $LOG_FILE para detalles de los errores.${NC}"
  fi
  echo -e "${BLUE}========================================${NC}"
}

# --- EJECUCIÓN PRINCIPAL ---

function main() {
  echo "--- Inicio de Instalación: $(date) ---" >"$LOG_FILE"
  echo -e "${BLUE}=== Iniciando Script de Instalación Interactivo ===${NC}"
  check_root

  if ask_user "¿Instalar Dependencias Core del Sistema? (Recomendado)"; then
    group_core_system
  fi

  group_i3_desktop
  group_appearance
  group_wezterm

  if ask_user "¿Instalar Entorno de Virtualización (KVM)?"; then
    group_virtualization
  fi

  group_docker
  group_flatpak_notes

  if ask_user "¿Configurar Herramientas de Desarrollo y Shell?"; then
    group_dev_tools
  fi

  group_network_security
  group_dotfiles

  if ask_user "¿Desactivar gestión de red predeterminada (Networking/WPA)? (Cuidado si es remoto)"; then
    run_install "Network Disable" "sudo systemctl stop networking && sudo systemctl disable networking && sudo systemctl disable wpa_supplicant"
  fi

  group_cleanup
  print_summary

  echo -e "${YELLOW}Pasos manuales requeridos:${NC}"
  echo "1. Reinicia el sistema."
  echo "2. Conectate al wifi con 'nmtui' si desactivaste wpa_supplicant."
  echo "3. Ejecuta 'zsh' para inicializar la shell."
  echo "4. Finaliza login en Dropbox, para sincronizar el vault de Obsidian y Discord."
}

trap "echo 'Script finalizado.'" EXIT

main "$@"
